import React from 'react';
import { connect } from "react-redux";

const NewCourseContainer = () => {
    return (
        <div>New Course</div>
    )
};

export default connect()(NewCourseContainer);